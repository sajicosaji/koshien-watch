# 甲子園見守りbot 一回限りの「今までの結果まとめ」実行

これは通常の日次実行ではなく、大会開始(8/5)から今日(8/9)までの**未確定分をまとめて確定させ、
1本の要約メッセージをDiscordに送る**、一回限りの特別実行です。無人実行なので質問はせず、
分かった範囲で最後まで実行してください。

## 手順

1. `bracket.json` を読む。

2. WebSearchで、2026年8月5日から今日までに**既に終了している試合**の結果（スコア・勝者）を確認する。
   今日(8/9)のうち、まだ試合開始時刻を迎えていない/終わっていない試合は無理に調べず「結果未確認」の
   ままでよい（例えば夕方の試合は今調べても終わっていない可能性が高い）。

3. 確定した試合について `bracket.json` を更新する（`winner`を埋める、次の試合の`team1`/`team2`に
   勝者を反映する、ownerを引き継ぐ）。前回の実行で埋まっていない「確認中」の出場校が判明していれば
   それも埋める。Writeで保存。

4. `bracket.json` の内容から、7人の担当者（伊藤・鵜瀬・河村・熊田・栗原・西條・西村）それぞれについて、
   現時点の「勝ち残り校数」と「敗退した学校名」を集計する。担当者が割り当てられていない
   （ownerがnullの）出場校があれば、それも別枠で報告する。

5. 1本にまとめたDiscordメッセージを作成し、以下のcurlコマンドで `DISCORD_WEBHOOK_KOSHIEN` に**1回だけ**POSTする：
   ```
   curl -sf -H "Content-Type: application/json" -d "$(jq -n --arg content "$MESSAGE" '{content:$content}')" "$DISCORD_WEBHOOK_KOSHIEN"
   ```
   メッセージ構成（見やすく簡潔に）：
   - タイトル的に「📋 甲子園 これまでの結果まとめ（8/5〜8/9）」と一言
   - 日付順に「学校A ○-○ 学校B（勝者に◯）」を列挙（スコア不明な既終了分は勝者のみでよい）
   - 担当者ごとの現在の勝ち残り数を一覧（例: 西條 7残、河村 4残(3敗)、のように）
   - 担当者未定の出場校があれば最後に一言添える
   - 煽り・誇張なしで事実だけ簡潔に。1メッセージに収まるようにする（長すぎる場合は結果一覧を優先し、
     担当者集計は数字だけに絞る）

6. `bracket.json` の `last_updated` を今日の日付にして保存する（手順3と合わせて1回でよい）。

## 環境変数
`DISCORD_WEBHOOK_KOSHIEN` は既に環境変数として渡されています。
