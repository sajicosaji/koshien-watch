# 甲子園見守りbot 実行指示

第108回全国高等学校野球選手権大会（2026/8/5〜8/22、阪神甲子園球場）を、7人の担当者
（伊藤・鵜瀬・河村・熊田・栗原・西條・西村）が持ち回りで応援するプールを見守るbotです。
毎日19:00(JST)に実行され、今日の試合結果をDiscordに知らせます。

## 手順

1. `bracket.json` を読む。これが唯一の正データ。各ブロックに1回戦〜3回戦の試合が並んでおり、
   各試合は `team1`/`team2`（`name`/`pref`/`owner`）と `winner`（勝者チーム名、未確定なら null）を持つ。
   `team1`/`team2` が null または `name` が「確認中」の試合は、まだ相手が確定していない。

2. 今日の日付（JST）を確認し、`bracket.json` の中で **今日の日付の試合で winner がまだ null** のものを
   すべて拾い出す。該当が無ければ「今日は対象カードなし」として手順6のみ行い、Discord投稿はしない
   （空振りの日に無言配信はしない）。

3. 該当する試合について、WebSearchで結果を調べる。情報源は日本高等学校野球連盟・朝日新聞・
   日刊スポーツ・スポーツ報知など複数の一次〜準一次情報を優先し、スコアと勝者を確定させる。
   test1/team2が「確認中」の試合（開幕前の代替校決定戦など）は、確定した出場校名も調べて埋める。

4. 確定した結果を `bracket.json` に反映する：
   - 該当試合の `winner` に勝者チーム名を設定
   - もしその試合が次の試合（3回戦や2回戦）の `team1`/`team2` に対応する場合（noteに「〇/〇の勝者」と
     書かれている枠）、勝者チームの `name`/`pref`/`owner` をそこにも埋める。owner は勝ち上がった側の
     元のownerをそのまま引き継ぐ。
   - 「確認中」だった出場校が判明した場合は `team1`/`team2` の `name`/`pref`/`owner` を埋め、noteを消す。

5. Discord向けメッセージを作成し、`DISCORD_WEBHOOK_KOSHIEN` にPOSTする。内容：
   - 今日確定した試合結果（スコアが分かれば併記、分からなければ勝者のみ）
   - 敗退したチームがいれば、そのownerの「持ち球」が1つ減ったことが分かるように書く
   - 全担当者について、現時点で勝ち残っているチーム数を一覧表示する（0校になった担当者は「敗退」と明記）
   - 煽りや誇張は不要。事実（誰が勝った・誰が何校残っているか）を簡潔に。

6. `bracket.json` の `last_updated` を今日の日付（JST, YYYY-MM-DD）に更新して保存する。

## 注意
- 雨天順延などでその日に試合が行われなかった場合は、試合が無かった旨だけ簡潔に記録し、
  `bracket.json` の日付・時刻は動かさない（順延の詳細が分かればnoteに一言追記する程度でよい）。
- 準々決勝以降の組み合わせは大会中の抽選で決まるため、3回戦までの勝者が全て決まった時点で
  改めて対応が必要になる。bracket.jsonに準々決勝以降のデータが無いことに気づいたら、
  Discordメッセージの最後に「準々決勝の組み合わせ抽選が必要です」と一言添える。
- 大会が終了し優勝校が決まったら、その旨をDiscordに大きく伝え、優勝校のownerを明記する。
